# Use the public npm registry by default
registry=https://registry.npmjs.org/

# Configure your private registry (replace with your actual URL)
@your-scope:registry=https://your-private-registry.example.com/

# (Optional) Add authentication for your private registry
# //your-private-registry.example.com/:_authToken=YOUR_AUTH_TOKEN